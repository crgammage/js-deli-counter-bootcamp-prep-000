var katzDeli = [];
function takeANumber(katzDeli, name) {
  katzDeli.push(`${name}`);
  return (`Welcome ${name} you are number ${katzDeli.length} in line.`)
}

function nowServing(katzDeli) {
  let i = 0;
  do {katzDeli.slice(0), i++}
  while (i < katzDeli)
}
